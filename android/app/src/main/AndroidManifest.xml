<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: افتح روابط الدعوة داخل التطبيق مباشرة بدون متصفح.
                 android:autoVerify يخلّي Android يتحقق من
                 https://12362aa.github.io/.well-known/assetlinks.json (يحوي بصمة
                 SHA-256 لهذا التطبيق) فيفتح الرابط في التطبيق فورًا بلا نافذة اختيار. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="12362aa.github.io"
                      android:pathPrefix="/chess" />
            </intent-filter>
        </activity>

        <!-- قناة الإشعار الافتراضية: لو رسالة FCM جت من غير channelId صريح،
             Firebase بيستخدم القناة دي بدل ما يقع على fallback ممكن يتبلع.
             نفس id القناة اللي العميل بيعملها (chess-amkh). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="chess-amkh" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- #151: خدمة FCM مخصّصة تورث خدمة إضافة Capacitor وتفوّض ليها كل
             الرسايل العادية، وتبني إشعار مكالمة بأزرار رد/رفض للـ kind=call.
             نشيل تسجيل خدمة الإضافة (نفس فلتر MESSAGING_EVENT) عشان خدمتنا هي
             الوحيدة اللي تستقبل، وهي أصلاً بتنادي super فمفيش تغيير في السلوك. -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <receiver
            android:name=".CallActionReceiver"
            android:exported="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- #151: إشعار المكالمة الواردة بملء الشاشة (مطلوب على أندرويد 14+) + اهتزاز -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Microphone: online voice chat (getUserMedia in the WebView).
         Capacitor's WebChromeClient maps the WebView audio-capture request to
         this runtime permission and prompts the user — without it declared,
         getUserMedia throws and voice recording fails with
         «لا يمكن الوصول للميكروفون». -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Camera: video calls (getUserMedia video in the WebView) #160.
         Capacitor's WebChromeClient.onPermissionRequest maps the WebView
         video-capture request to this runtime permission and prompts the
         user — without it declared getUserMedia({video}) throws NotAllowed.
         camera hardware is optional so the app still installs on cameraless
         devices (audio calls keep working). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <!-- Bluetooth Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Android 12+ Bluetooth Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- WiFi Permissions for Nearby Connections -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
</manifest>
